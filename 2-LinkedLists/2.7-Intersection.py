from linkedlist import *


def get_intersection_node(headA, headB):
    len_headA = get_length(headA)
    len_headB = get_length(headB)

    if


def get_length(head):
    length = 0

    while head != None:
        length += 1
        head = head.next
    
    return length